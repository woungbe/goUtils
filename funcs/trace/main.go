package main

import "woungbe.utils/funcs/trace/lib"

// Trace returns a string with information about the call stack up to a specified depth.

func main() {
	// Example usage of Trace function
	// fmt.Println(Trace(3)) // Trace the call stack up to 3 levels

	lib.ExampleFunc()
}
